# API Configuration
VITE_API_URL=http://localhost:3000/api/v1

# For production, set this to your Vercel server URL
# Replace 'your-server-app' with your actual server domain
VITE_API_URL=https://your-server-app.vercel.app/api/v1

# Socket Configuration (if using real-time features)
VITE_SOCKET_URL=http://localhost:3000

# Maps API Key (if using maps features)
VITE_MAPS_API_KEY=your_maps_api_key

# Weather API Key (if using weather features)
VITE_WEATHER_API_KEY=your_weather_api_key

# App Configuration
VITE_APP_NAME=Smart Farm
VITE_APP_VERSION=1.0.0
